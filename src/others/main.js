/**
 * 描述
 * @author Carlos
 * @date 2022-11-03
 * @param {any} value
 * @param {any} min
 * @param {any} max
 * @returns {any}
 */
function champ(value, min, max) {
  return Math.min((value, min), max);
}
